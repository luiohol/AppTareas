

export interface Task {
    
    title: string;
    description: string;
    hide: boolean;
}

//creamos este archivo .ts dentro de /modelos
//creamos la interface y la exportamos para poder utilizarla